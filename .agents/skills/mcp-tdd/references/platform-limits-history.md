# 平台限制:已滚出的历史行（**只读**，别再往这里加行）

本文件是 `platform-limits.md` 的**滚动归档**：主表撞 20 KB 上限时，把「**代码已兜住**且长期未复发」
的行搬到这里冻结。搬进来**不是注销** —— 现象与结论仍然有效，只是不再占用主表的常驻体积；
要是哪天复发，把它**整行搬回主表**并更新口径（别在这里改）。

> 与台账的 `archive` 命令（把已终结的报错事件搬出主库）是两件事，别混。
> 口径与触发条件见 `platform-limits.md` 的「体积治理」，仓库决策见
> `docs/design-decisions/0008-*.md`。

| 现象 | 根因 | 处理口径 | 代码落点 |
|---|---|---|---|
| 对实例子节点改 `fills` / `fontName` 等样式，回显是新值但渲染不变 | 引擎不支持实例子节点样式 override 持久化（`characters` 文字内容正常） | **平台限制，不修**。改为「检测 + 点名 + 给出口」：写样式类字段到 INSTANCE 内子节点时返回 `warnings` 并算出主组件对应子节点 id；样式类工具描述统一挂 `INSTANCE_STYLE_WARN` | `shared/src/core/update.ts`（`instanceStyleFixHint()`） |
| 创建 `LINE` 传 `height: 0` 被 `resize` 拒绝（`>= 0.01`） | 存储侧允许零轴、创建路径校验不允许 | 创建路径对 `type === 'LINE'` 做零轴豁免，其余类型行为不变 | `shared/src/core/buildNode.ts` |
| 只传 `strokes` 的描边图形被自动加灰底 | 未显式传 `fills` 时落到引擎默认值（子图形默认灰底，与文档不符） | 未传 `fills` 但传了 `strokes` 且非 FRAME → 显式 `node.fills = []`；FRAME 仍走默认白底 | `shared/src/core/buildNode.ts` |
| 迁入 auto-layout 容器报 `in get_layoutGrow: ... 'jsGet'` | auto-layout 父容器上 `insertChild` 会触发引擎布局重算，读子节点 `layoutGrow` 崩溃。原注释「insertChild 比 appendChild 更可靠」**与实测相反** | 父容器 `layoutMode !== 'NONE'` 时改用 `appendChild`；非 auto-layout 保持 `insertChild` 以保留 index 语义 | `shared/src/core/nodes.ts` |
| `jsd_ping` 的 `capabilities` 只有 `["styles"]`，看着像漏报 | 该表列的是**平台差异超集能力**，jsDesign 确实一项都不支持，只报 `styles` 是准确的 | 新增 `coreCapabilities`（create / modify / structure / component / export / image）表达平台无关核心能力，无需各 adapter 重复声明 | `shared/src/schemas/platform.ts`、`ui/src/code/plugin.ts` |
| 实例侧序列化读到空 `fills` | 实例 override 可能未落盘 | **不要据此判定样式缺陷**，先查主组件 | —— |
| `fontName` 写了不存在的组合（把 style 写成简称 `"Bold"` / 写一个不存在的族）→ 渲染退回默认字面，回显却看不出任何异常（中文字重看起来「Bold 不生效」） | 引擎只对**能解析**的 (family, style) 做规范化；解析不了的组合**原样保留请求值**（`loadFontAsync` 失败被吞、赋值不报错），字面差异只在渲染上暴露（实测：请求 `{SourceHanSansCN_family, "Bold"}` 时回读照抄，渲染与 Regular 逐字节相同） | **是写法问题，不是平台限制**（2026-09-19 实测）：`family` 取 `jsd_list_fonts` 的 `fonts[].family`、`style` 取同一项的 `fonts[].styles` **全名**（`SourceHanSansCN-Bold`）。用对写法后 Bold/Regular 导出 PNG 的 sha1 **不同**（3381 vs 3406 字节）＝字重真生效。**代码已兜住**：请求带 `_family` 而结果里**仍带** `_family`（或族被换成别的、style 落成 `@@` 哨兵）→ 进结果 `warnings` 点名并带本次实测形态 | `shared/src/core/utils.ts`（`fontNotResolved`）、`core/execute.ts` / `core/update.ts`（**结果装配期**判，必须用序列化后的值）、`dicts/unapplied-prop.ts` |
| `jsd_list_fonts` 回显的 family 带 `_family` 后缀（`MiSans_family` / `SourceHanSansCN_family`，少数无后缀如 `IdeaFonts JiXieTi`） | **已定论（2026-09-19 实测）**：那是平台列表里的原始 family 名，可直接写进 `fontName`；解析成功 = 引擎把它**规范化**成短名（`SourceHanSansCN_family` + `SourceHanSansCN-Bold` → 落库 `{family:"SourceHanSansCN", style:"Bold"}`） | ① 照 `jsd_list_fonts` 原样取用；② 「有没有被规范化」是解析与否的唯一可靠信号，但它**只在结果序列化时可见** —— 写入/结算瞬间回读到的还是原样回显，在那个时机判会把合法写法误报成没生效（实测踩过，见 0007），故判定落在结果装配期 | `shared/src/core/export.ts`（`fonts:[{family,styles}]`）、`shared/src/core/utils.ts`（`fontNotResolved`） |
| 显式 `width` + `textAutoResize:"HEIGHT"` 被压回 `NONE`（create 路径） | 同上：尺寸回压在文本字段之后执行 | 同上（`textStabilizeWriter` 已修）；修改路径单设 `HEIGHT` 本来就正常（实测回读 `HEIGHT`） | 同上 |
| MasterGo:文本样式读不出来(创建时写了 `fontSize`/`fontName`,回读却没有该字段) | MG 的 `TextNode.textStyles` 是**分段样式表**,字号/字体/行高/字距/大小写/装饰在段的**嵌套**对象里(`seg.textStyle.fontSize`),只有 `textStyleId` 在段顶层;按顶层读恒 `undefined` | **代码已兜住**:门面按嵌套结构读(`node-facade.ts` 的 `readTextProp`),分段值不一致仍回混合哨兵 | `ui/src/code/mastergo/node-facade.ts` |
| 建根节点时显式给的 `x/y` 不生效、落到别处(实测传 `(2400,0)` 落在 `(-504,-315)`) | 本仓语义:`placement` 缺省 `center`,用视口中心覆盖根节点自身的 `x/y` | **代码已兜住**:给了根 `x/y` 又没声明 `manual`/`absolute` 时进结果 `warnings`,点名两种正确姿势 | `shared/src/core/execute.ts`、`core/props/outcome.ts` |
| 建 TEXT 时给 `width`(不给 `textAutoResize`):宽度生效、文本确实换行,但 `textAutoResize` 被置成 `NONE`、**文本框高度停在单行高** | 引擎的 `resize()` 会把 `textAutoResize` 重置为 `NONE`,而 `NONE` 下高度不随换行重算 | **代码已兜住**:① 显式声明过的值由 `textStabilizeWriter` 在尺寸之后再压一遍;② 未声明却被置 `NONE` 时进结果 `warnings` | `shared/src/core/props/writers.ts`、`core/buildNode.ts`、`dicts/unapplied-prop.ts` |
| 文本里的 `▾`(U+25BE) / `▸`(U+25B8) 等几何箭头渲染成方块(豆腐块) | `SourceHanSansCN` 无该字形,引擎不做字体回退 | **平台限制,调用方换实现**:装饰性箭头改用 `jsd_create_icon` 独立图标节点,不要塞进文本 `characters` | —— |
| `jsd_batch` 里用 `{{步骤id.字段路径}}` 引用**上一批**的步骤 | 步骤表是单次调用的局部状态,跨批次必然查不到 | 报错原文「占位符引用的步骤不存在或未成功」并中止整批 → **跨批次硬编码上一步回显里的真实 id**;该边界已写进 `jsd_batch` 描述、`server.ts` INSTRUCTIONS 与脚本化配方 | `mcp-server/src/tools/batch.ts` |
| 按 `jsd_manage_nodes` 的聚合清单写占位符,`group` / `flatten` 报「无法解析占位符引用」 | 这两个 op 返回**单对象** `{created:{…}}`,`clone` / `outline_stroke` 才是数组 | 描述已改正;**教训**:同一份「返回键形状」手写四处必然漂移 | `mcp-server/src/tools/{batch,manage}.ts`、`server.ts`、`README.md` |
| `jsd_create_frame` 创建时传 `primaryAxisAlignItems`/`counterAxisAlignItems` 被静默忽略(节点落成 MIN/MIN) | `layoutWriter` 的 create 分支只写 layoutMode/itemSpacing/padding + sizingMode 推断,对齐两键从未落引擎 | **代码已兜住**:create 分支补写对齐;settle 阶段与 layoutMode 同口径「写 → 回读 → 不一致再压一次」 | `shared/src/core/props/writers.ts`(`layoutWriter`) |
| 凡流向 MasterGo 的 **RGBA 都必须带 `a`**（缺则整条被静默丢弃） | MG 的 `SolidPaint.color` / `ColorStop.color` / `Effect.color` / 网格 `color` 都是 `RGBA`（`a` 必填，且注明「纯色模式下 alpha 恒为 1，请设置 color 中的 alpha」），而线格式允许只给 `{r,g,b}` | **代码已兜住**：收成一个 `withAlpha()`（`core/normalize` 的三个 RGBA 落点共用）+ 门面 `writeColor()`（SOLID / 渐变停靠点 / 网格三处共用）。此前**按字段名逐点补**：paint 补了、effect 与 grid 漏了 —— 故改成统一换算 | `shared/src/core/normalize.ts`（`withAlpha`）、`ui/src/code/mastergo/node-facade.ts`（`writeColor`） |
| `figma_variables_create` 在 dynamic-page（增量模式）下报 `in createVariable: Cannot call createVariable with a collection id in incremental mode` | 增量模式的 `figma.variables.createVariable` 拒收 collection **id**，必须传 collection **节点**；legacy 两种都收（0011 实测撞上，指纹 `b300df0ec236`） | **代码已兜住**：统一传 collection 节点（两种模式都接受）；同名集合复用分支传的就是节点对象，不受影响 | `ui/src/code/figma/ops.ts`（`createVariables`） |
| MasterGo：`effects` 里的阴影/模糊不落盘（回读无该字段） | MG 的 `ShadowEffect` / `BlurEffect` 必填字段比本仓多、且字段名不同：可见性是 `isVisible`，阴影还要 `spread` / `blendMode` / `showShadowBehindNode`，模糊另有必填 `gradient`（MG 的渐进模糊模型） | **代码已兜住**：门面按 MG 模型补默认值（`MG_SHADOW_DEFAULTS` / `MG_BLUR_DEFAULTS` / `MG_BLUR_GRADIENT` 三张表）；三张表**有编译期断言**钉在 typings 上（官方改必填集合会 `tsc` 红，不必等真机「阴影又不落」）；读时丢掉 MG 专有的 `isEffectShow` | `ui/src/code/mastergo/node-facade.ts`、`ui/src/code/mastergo/sync-guarantee.ts` |
| MasterGo：换绑（swap）**不改实例自身尺寸** —— 拿它当判据会把「成功」误判成「没生效」 | 实例换绑到尺寸不同的组件后，读该实例 `width` 仍是原值（实测：60 宽实例换到 140 宽组件，读回仍 60）；变的是**内部子层** | **判据读内部子层**：`mg_list_sublayers` 看里面矩形的 `name`/`width`（实测 SW3-A/60 ↔ SW3-B/140）。本仓已因此误判过一次，见 0019 的 INSTANCE_SWAP 专项 | `ui/src/code/mastergo/ops.ts`（`mg_list_sublayers`） |
| MasterGo：`jsd_set_instance_properties` 传变体值**回包成功、画布不变**（静默失效） | MG 运行时对变体值的两条原生入口（`setProperties` / `setVariantPropertyValues`）都不生效且不报错；且 `componentProperties[].id` 运行时不存在，按 propertyId 写也走不通 | **代码已兜住**：变体写入改成**集合内换绑**（在同一 `COMPONENT_SET` 里找变体值全等匹配的成分 `swapComponent` 过去），匹配不到**明确报错**并列出可用组合、换绑后**回读校验**（见 0018）。调用面不变。**真机复验通过** | `ui/src/code/mastergo/variant-swap.ts`、`node-facade.ts`（`applyInstanceProps`） |
| MasterGo：绑定属性报 `Can only set component property references on symbol sublayer` | 绑定目标必须是**组件内部的图层**；而本仓 `create_component` **只建空壳**（子层由调用方 reparent 归组），空组件里当然没有「symbol sublayer」 | **正确用法**：`create_component` 之后把源节点 `reparent` 进组件再绑 —— 照做后绑定与写入**都正常**（实测）。教训：见报错先确认前置条件，别急着归因平台（本轮误判过一次，已撤回） | `shared/src/core/component.ts`（`createComponentNodes`） |
| MasterGo：造第二个**变体值**（联调用法，不是缺陷） | 变体值由**成分名字**驱动：把集合里的成分改名为 `属性 1=备选`，MG 归一成 `属性 1[a1]=备选` 并把该成分的 `variantProperties` 置为 `备选` | 工具面没有 `createVariantProperties`/`editVariantPropertyValues` 入口（typings 有、工具面没接），这是目前唯一能在真机上造值的办法 —— 测变体切换时先这样造值 | `jsd_rename_node`（无需代码兜住） |
| MasterGo：实例侧 `componentProperties` **恒为空表 / 不可靠**（同一实例写前报得出、写后回空表） | 实例属性值不落在实例自身的属性表里；该表在 MG 上不稳定 —— 而**绑定的图层确实跟着改了**（实测 `characters`/`isVisible` 都变） | **别拿它判写失败**（会假阴性）：要观测「属性是否生效」读**被绑定的图层** —— `mg_list_sublayers` 回读 `characters`/`isVisible`；门面只在明显不一致时留开发者日志 | `ui/src/code/mastergo/node-facade.ts`（`applyInstanceProps`）、`ops.ts` |
| 往 auto-layout 容器**带 `index`** 插子节点后，容器的**显式尺寸被吃掉**（实测 `height: 400` 的 VERTICAL 容器插入 1px 子节点后变 `height: 1`；**对照组**同一容器改走 `appendChild` 不触发） | `insertChildAt` 的 `NONE ↔ 方向` 往返让引擎**重进** auto-layout；写回的 `primaryAxisSizingMode` 只是「回显是新值」——回读先显示 `FIXED`，再做一次布局操作就变回 `AUTO` | **代码已兜住**：恢复顺序固定为「方向 → 其余布局属性 → 尺寸」，且每一步都回读校验；快照里该轴为 `FIXED` 的，收尾用 `resize` 把尺寸压回去 | `shared/src/core/nodes.ts`（`restoreLayoutVerified` / `pinnedSizeOf` / `applyPinnedSize`） |
| MasterGo：组件侧 `componentPropertyValues` 为**空**（带文本子节点的组件也读不到 TEXT 属性） | MG 不会把组件的子文本图层自动暴露成 `TEXT` 组件属性；新增属性只能靠 `addComponentProperty` | **已有平台 op 兜住**：`mg_add_component_property` / `mg_edit_component_property` / `mg_delete_component_property`（见 0019）—— 建好属性再用 `jsd_set_instance_properties` 写实例值。**别再让用户去设计器里手加** | `ui/src/code/mastergo/ops.ts` |
| MasterGo：改字号/行高报 `Cannot use unloaded font "PingFang SC Regular". Please call mg.loadFontAsync(...)` | MG 要求该段**当前字体**已加载才允许写文本样式；而「新建文本且调用方没给 `fontName`」时读不到当前字体（`textStyles` 为空），它自己的默认字体又**不在可用清单里**（实测 1850 族 `PingFang` 0 命中）→ 前置条件无法满足 | **代码已兜住**：字体加载失败不再吞（`loadFont` 返回结果），写不进去就进结果 `warnings` 点名，并提示「取 `jsd_list_fonts` 的 `fonts[]` 成对值传 fontName」；内容照写、样式跳过，**不再让整次创建炸在引擎错误上** | `shared/src/core/props/writers.ts`（`textWriter`）、`shared/src/core/utils.ts`（`loadFont`） |
| 显式传 `layoutMode`（如 `HORIZONTAL`），回读却是另一方向（`VERTICAL`）→ 子节点全叠在同一点；**真实触发条件未能复现**（单子 / 双子 reparent、原批次 8 步忠实复刻、3 组创建参数对照，全部正常） | 未定论。同族证据指向「引擎在布局重算后会回写容器布局属性」 | **代码已兜住**：三处写 `layoutMode` 的落点统一改走「写 → 回读 → 不一致再压一次」；`set_layout` 压不住时进 `warnings` 点名 | `shared/src/core/utils.ts`（`ensureLayoutMode`）、`buildNode.ts`、`update.ts`（`set_layout` 回读 + `warnings`）、`nodes.ts` |
| MasterGo：给容器写 `layoutGrids` 回包「已更新 1 个节点」、回读没有该字段，**且同会话怎么读都读不到**（同一节点同一条写路径的 `fills`/`effects`/`clipsContent` 全部正常） | 引擎行为，两个事实各自独立：① 节点级 `layoutGrids` 写入**偶发落地** —— 同一份参数逐帧连写 8 帧**只成 1 条**，而落地那条的值**完整正确**（`pattern`/`count`/`gutterSize`/`alignment`/`color` 逐项对得上，引擎另补默认 `sectionSize`）⇒ 门面的字段名与值域映射（`pattern↔gridType`、`MIN→LEFT`、RGBA 带 `a`）**没问题，方差全在平台侧**；② **可见性同样不可靠**：唯一落地那条**同会话回读为空**（数分钟后才读到），后续复验又出现两种**反向**不一致 —— 某容器先能读到、之后同一 id 又读不到；跨插件重载后再次消失。⇒ 「写完立即回读」在 MG 上**不能当判据**，也不能假定它稍后会稳定下来。MS 官方文档另有一条：布局网格与自动布局互斥（「布局网格只有在非自动布局时设置才会生效」，实测无自动布局的帧同样不落地，故互斥不是唯一成因） | **代码已兜住「不许静默」**：`layoutGrids` 写后回读校验，不匹配即在结果 `warnings` 里点名「请求 N 条 / 回读 M 条」，并**区分**「写入未发生（存在性守卫跳过）」与「写入已发出但引擎未保留」；MG 侧走专用口径，明说本条**不必然代表写失败**、要求以画布目检为准、建议在 MG 界面手动加网格（**别据此重试刷屏**）。工具面不新增 MG 网格 op：`createGridStyle` 需要「已有网格的节点」当输入，节点级写不进去时它同样无从下手 | `shared/src/core/props/writers.ts`（`verifyLayoutGrids`）、`shared/src/__tests__/write-path.test.ts` |
