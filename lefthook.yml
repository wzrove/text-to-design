pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,tsx,mjs,cjs,json,jsonc}"
      run: pnpm exec biome check --write --unsafe   {staged_files}
    typecheck:
      run: pnpm typecheck

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit "$1"
