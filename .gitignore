# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.opencode

# skill 触发率评测的产物（查询集与脚本要提交，运行结果不提交）
.mcp.json

# MCP 报错台账：本机数据，不进仓库。
# 事件流（errors.jsonl）会随任务无限增长；闭环库 / 用例 / run 元信息只对跑过它的那台机器有意义。
# CLI 首用时自动重建目录与 README（见 .agents/skills/mcp-tdd/scripts/mcp-tdd.mjs 的 init/ensure），
# 所以新克隆的仓库不需要它也能正常工作。
docs/mcp-errors/
